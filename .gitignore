# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
server/node_modules
client/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# production
/build

# misc
.DS_Store
Thumbs.db
*.swp
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env.local
server/.env

# vercel
.vercel

# typescript
*.tsbuildinfo

# SQLite Database
*.db
*.db-journal

# Drizzle Kit session token (if it creates one locally)
# .drizzle-kit-token (or similar, check if drizzle kit creates local temp files)